# Provenance

`package-lock.json` is the unmodified output of `npm install --package-lock-only`
(npm 11.17.0) run on a dependency-less package.json:

```json
{
  "name": "ublacklist",
  "version": "10.0.2",
  "private": true
}
```

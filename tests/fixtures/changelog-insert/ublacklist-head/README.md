# Provenance

`CHANGELOG.md` is the head of the real `iorate/ublacklist` CHANGELOG.md (generated by the original `@changesets/cli`), fetched via `gh api repos/iorate/ublacklist/contents/CHANGELOG.md` on 2026-07-30 and truncated just before the `### Minor Changes` heading of the `## 10.0.0` section.
